[project]
name = "mai_pal_backend"
version = "0.1.0"
description = "Backend for Mai Pal App"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "sqlalchemy",
    "psycopg2-binary",
]
